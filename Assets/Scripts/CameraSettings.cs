public struct CameraOrbits
    {
        public float Height;
        public float Radius;

        public CameraOrbits(float height, float radius)
        {
            Height = height;
            Radius = radius;
        }
    }
